import PagePlaceholder from "@/components/layout/PagePlaceholder";

export default function FleetPage() {
  return (
    <PagePlaceholder
      eyebrow="Vehicle Registry"
      title="Fleet Registry"
      description="Maintain vehicles with registration numbers, model details, load capacity, odometer readings, acquisition cost, status, and region."
    />
  );
}
