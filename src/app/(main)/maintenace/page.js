import PagePlaceholder from "@/components/layout/PagePlaceholder";

export default function MaintenacePage() {
  return (
    <PagePlaceholder
      eyebrow="Maintenance"
      title="Maintenance"
      description="Create active maintenance records, move vehicles into the shop, and restore them to available status when service closes."
    />
  );
}
