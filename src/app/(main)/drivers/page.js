import PagePlaceholder from "@/components/layout/PagePlaceholder";

export default function DriversPage() {
  return (
    <PagePlaceholder
      eyebrow="Driver Management"
      title="Drivers"
      description="Track driver profiles, license validity, contact details, safety scores, duty status, and compliance blockers."
    />
  );
}
