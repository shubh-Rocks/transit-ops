import PagePlaceholder from "@/components/layout/PagePlaceholder";

export default function TripsPage() {
  return (
    <PagePlaceholder
      eyebrow="Trip Management"
      title="Trips"
      description="Create and monitor trips through draft, dispatched, completed, and cancelled states with vehicle, driver, cargo, and distance validation."
    />
  );
}
