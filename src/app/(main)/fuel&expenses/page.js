import PagePlaceholder from "@/components/layout/PagePlaceholder";

export default function FuelExpensesLegacyPage() {
  return (
    <PagePlaceholder
      eyebrow="Fuel & Expense Management"
      title="Fuel & Expenses"
      description="Record fuel logs, tolls, maintenance spend, and other expenses that feed operational cost analytics."
    />
  );
}
