import PagePlaceholder from "@/components/layout/PagePlaceholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      eyebrow="Workspace"
      title="Settings"
      description="Manage TransitOps preferences, workspace configuration, and account controls."
    />
  );
}
